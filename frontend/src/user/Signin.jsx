import React from "react";
import Base from "../core/Base";

const Signin = () => {
  return (
    <Base title="Signin page" description="A page for user signin">
      <h1>Signin page works</h1>
    </Base>
  );
};

export default Signin;
